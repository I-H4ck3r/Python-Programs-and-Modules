from tkinter import*
root = Tk()
root.geometry("400*400")          #default size,can be changed big/small
root.resizable(0,0)               #default size,can't be changed bcz resize is 0
root.mainloop()

#root.minsize(500,500)
#least size of window, can't be smaller
#root.maxsize(2000,2000)
#highest size of window, can't be bigger
